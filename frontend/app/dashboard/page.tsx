'use client'; import {useEffect,useState} from 'react'; import {api} from '../../lib/api'; import {Shell} from '../_components';
export default function Dashboard(){const [d,setD]=useState<any>(null);useEffect(()=>{api('/dashboard').then(setD).catch(()=>location.href='/login')},[]);return <Shell title="Operations Dashboard">{!d?<p>Loading...</p>:<><div className="grid"><Card n={d.assets} t="Tracked Assets"/><Card n={d.vendors} t="Active Vendors"/><Card n={d.openPurchaseOrders} t="Open POs"/><Card n={d.maintenance} t="Maintenance"/><Card n={d.expiringSoon} t="Expiring ≤ 90 days"/></div><div className="card section"><h3>Recent Asset Movement</h3><table className="table"><thead><tr><th>Asset</th><th>Movement</th><th>From</th><th>To</th><th>Date</th></tr></thead><tbody>{d.recentMovements.map((m:any)=><tr key={m.id}><td>{m.asset.assetTag}</td><td><span className="badge">{m.type}</span></td><td>{m.fromLocation?.name||'—'}</td><td>{m.toLocation?.name||'—'}</td><td>{new Date(m.movedAt).toLocaleDateString()}</td></tr>)}</tbody></table></div></>}</Shell>}; function Card({n,t}:{n:any,t:string}){return <div className="card"><div className="muted">{t}</div><div className="metric">{n}</div></div>}
