import {Router} from 'express';import multer from 'multer';import {mkdirSync,unlink} from 'node:fs';import {randomUUID} from 'node:crypto';import path from 'node:path';import {prisma} from '../db.js';import {auth,authorize,AuthRequest} from '../middleware/auth.js';import {audit} from '../audit.js';import {AppError} from '../middleware/errors.js';
const uploadDir=path.resolve(process.cwd(),'uploads');mkdirSync(uploadDir,{recursive:true});const allowed=new Set(['application/pdf','image/jpeg','image/png','image/webp','text/plain','application/msword','application/vnd.openxmlformats-officedocument.wordprocessingml.document']);const upload=multer({storage:multer.diskStorage({destination:uploadDir,filename:(_req,file,cb)=>cb(null,`${randomUUID()}${path.extname(file.originalname).toLowerCase()}`)}),limits:{fileSize:10*1024*1024},fileFilter:(_req,file,cb)=>cb(null,allowed.has(file.mimetype))});
export const documentsRouter=Router();documentsRouter.use(auth);
documentsRouter.post('/assets/:assetId',authorize('SUPER_ADMIN','ORG_ADMIN','ASSET_MANAGER','MAINTENANCE','STORE_MANAGER'),upload.single('file'),async(req:AuthRequest,res)=>{const assetId=String(req.params.assetId),asset=await prisma.asset.findFirst({where:{id:assetId,organizationId:req.user!.organizationId}});if(!asset){if(req.file)unlink(req.file.path,()=>{});throw new AppError(404,'Asset not found')}if(!req.file)throw new AppError(400,'Choose an allowed file up to 10 MB');const type=String(req.body.type||'OTHER').slice(0,50);const row=await prisma.assetDocument.create({data:{assetId,type,name:req.file.originalname,url:'',storageKey:req.file.filename,mimeType:req.file.mimetype,size:req.file.size,organizationId:req.user!.organizationId,uploadedByUserId:req.user!.id}});const updated=await prisma.assetDocument.update({where:{id:row.id},data:{url:`/api/documents/${row.id}/download`}});await audit(req,'UPLOAD','AssetDocument',row.id,undefined,{name:row.name,type:row.type,size:row.size});res.status(201).json(updated)});
documentsRouter.get('/:id/download',async(req:AuthRequest,res)=>{const row=await prisma.assetDocument.findFirst({where:{id:String(req.params.id),organizationId:req.user!.organizationId}});if(!row||!row.storageKey)throw new AppError(404,'Document not found');res.download(path.join(uploadDir,path.basename(row.storageKey)),row.name)});
documentsRouter.delete('/:id',authorize('SUPER_ADMIN','ORG_ADMIN','ASSET_MANAGER'),async(req:AuthRequest,res)=>{const id=String(req.params.id),row=await prisma.assetDocument.findFirst({where:{id,organizationId:req.user!.organizationId}});if(!row)throw new AppError(404,'Document not found');await prisma.assetDocument.delete({where:{id}});if(row.storageKey)unlink(path.join(uploadDir,path.basename(row.storageKey)),()=>{});await audit(req,'DELETE','AssetDocument',id,{name:row.name,type:row.type});res.status(204).end()});
