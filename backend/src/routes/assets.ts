import { Router } from 'express'; import { z } from 'zod'; import { prisma } from '../db.js'; import { auth,authorize, AuthRequest } from '../middleware/auth.js'; import { audit } from '../audit.js'; import { AppError } from '../middleware/errors.js';
export const assetsRouter=Router(); assetsRouter.use(auth);
assetsRouter.get('/',async(req:AuthRequest,res)=>{ const q=String(req.query.q||''); const rows=await prisma.asset.findMany({where:{organizationId:req.user!.organizationId,OR:q?[{assetTag:{contains:q,mode:'insensitive'}},{serialNumber:{contains:q,mode:'insensitive'}},{product:{name:{contains:q,mode:'insensitive'}}}]:undefined},include:{product:true,vendor:true,currentLocation:true,department:true,category:true,custodian:{select:{id:true,name:true,email:true}}},orderBy:{createdAt:'desc'}}); res.json(rows); });
assetsRouter.get('/options/all',async(req:AuthRequest,res)=>{const org=req.user!.organizationId;const [products,categories,vendors,departments,locations,users,purchaseOrders]=await Promise.all([prisma.product.findMany({where:{organizationId:org},orderBy:{name:'asc'}}),prisma.assetCategory.findMany({where:{organizationId:org},orderBy:{name:'asc'}}),prisma.vendor.findMany({where:{organizationId:org},orderBy:{name:'asc'}}),prisma.department.findMany({where:{organizationId:org},orderBy:{name:'asc'}}),prisma.location.findMany({where:{organizationId:org},orderBy:{name:'asc'}}),prisma.user.findMany({where:{organizationId:org},select:{id:true,name:true,email:true},orderBy:{name:'asc'}}),prisma.purchaseOrder.findMany({where:{organizationId:org},select:{id:true,poNumber:true},orderBy:{poDate:'desc'}})]);res.json({products,categories,vendors,departments,locations,users,purchaseOrders})});
assetsRouter.get('/:id',async(req:AuthRequest,res)=>{ const row=await prisma.asset.findFirst({where:{id:String(req.params.id),organizationId:req.user!.organizationId},include:{product:true,category:true,vendor:true,currentLocation:true,department:true,custodian:{select:{id:true,name:true,email:true}},purchaseOrder:true,movements:{include:{fromLocation:true,toLocation:true},orderBy:{movedAt:'desc'}},assignments:{include:{user:{select:{id:true,name:true,email:true}}},orderBy:{assignedAt:'desc'}},maintenance:true,documents:true}}); if(!row)return res.status(404).json({message:'Not found'});res.json(row);});
const optionalDate=z.preprocess(v=>v===''?null:v,z.coerce.date().nullable().optional());const optionalId=z.preprocess(v=>v===''?null:v,z.string().cuid().nullable().optional());const assetSchema=z.object({assetTag:z.string().trim().min(2),serialNumber:z.string().trim().optional(),qrValue:z.string().trim().optional(),status:z.enum(['IN_STOCK','IN_USE','UNDER_MAINTENANCE','TRANSFER_PENDING','EXPIRED','DISPOSED','LOST']).optional(),purchasePrice:z.preprocess(v=>v===''?null:v,z.coerce.number().nonnegative().nullable().optional()),purchaseDate:optionalDate,commissioningDate:optionalDate,warrantyEndDate:optionalDate,expiryDate:optionalDate,notes:z.string().max(2000).optional(),productId:z.string().cuid(),categoryId:z.string().cuid(),vendorId:optionalId,purchaseOrderId:optionalId,departmentId:optionalId,currentLocationId:optionalId,custodianId:optionalId});
assetsRouter.post('/',authorize('SUPER_ADMIN','ORG_ADMIN','ASSET_MANAGER','STORE_MANAGER'),async(req:AuthRequest,res)=>{const b=assetSchema.parse(req.body),org=req.user!.organizationId;await validateRefs(b,org);const asset=await prisma.$transaction(async tx=>{const created=await tx.asset.create({data:{...b,organizationId:org,qrValue:b.qrValue||`ASSET:${b.assetTag}`}});await tx.assetMovement.create({data:{assetId:created.id,type:'RECEIPT',toLocationId:created.currentLocationId,movedByUserId:req.user!.id,remarks:'Asset created'}});if(created.custodianId)await tx.assetAssignment.create({data:{assetId:created.id,userId:created.custodianId,assignedByUserId:req.user!.id,remarks:'Initial assignment'}});return created});await audit(req,'CREATE','Asset',asset.id,undefined,asset);res.status(201).json(asset);});
assetsRouter.put('/:id',authorize('SUPER_ADMIN','ORG_ADMIN','ASSET_MANAGER','STORE_MANAGER'),async(req:AuthRequest,res)=>{const id=String(req.params.id),before=await prisma.asset.findFirst({where:{id,organizationId:req.user!.organizationId}});if(!before)throw new AppError(404,'Asset not found');const b=assetSchema.parse(req.body);await validateRefs(b,req.user!.organizationId);const row=await prisma.$transaction(async tx=>{const updated=await tx.asset.update({where:{id},data:b});if(before.currentLocationId!==updated.currentLocationId)await tx.assetMovement.create({data:{assetId:id,type:'TRANSFER',fromLocationId:before.currentLocationId,toLocationId:updated.currentLocationId,movedByUserId:req.user!.id,remarks:'Location changed while editing asset'}});if(before.custodianId!==updated.custodianId){await tx.assetAssignment.updateMany({where:{assetId:id,returnedAt:null},data:{returnedAt:new Date()}});if(updated.custodianId)await tx.assetAssignment.create({data:{assetId:id,userId:updated.custodianId,assignedByUserId:req.user!.id,remarks:'Custodian assigned'}})}return updated});await audit(req,'UPDATE','Asset',id,before,row);res.json(row)});
assetsRouter.delete('/:id',authorize('SUPER_ADMIN','ORG_ADMIN','ASSET_MANAGER'),async(req:AuthRequest,res)=>{const id=String(req.params.id),before=await prisma.asset.findFirst({where:{id,organizationId:req.user!.organizationId}});if(!before)throw new AppError(404,'Asset not found');await prisma.asset.delete({where:{id}});await audit(req,'DELETE','Asset',id,before);res.status(204).end()});
const moveSchema=z.object({toLocationId:z.string().cuid().nullable(),type:z.enum(['RECEIPT','ISSUE','TRANSFER','RETURN','MAINTENANCE','DISPOSAL','PHYSICAL_VERIFICATION']).default('TRANSFER'),remarks:z.string().trim().max(1000).optional()});
assetsRouter.post('/:id/move',authorize('SUPER_ADMIN','ORG_ADMIN','ASSET_MANAGER','STORE_MANAGER','MAINTENANCE'),async(req:AuthRequest,res)=>{const a=await prisma.asset.findFirst({where:{id:String(req.params.id),organizationId:req.user!.organizationId}});if(!a)throw new AppError(404,'Asset not found');const data=moveSchema.parse(req.body);if(data.toLocationId&&!await prisma.location.findFirst({where:{id:data.toLocationId,organizationId:req.user!.organizationId}}))throw new AppError(400,'Destination does not belong to your organization');const updated=await prisma.$transaction(async tx=>{const row=await tx.asset.update({where:{id:a.id},data:{currentLocationId:data.toLocationId,status:data.type==='MAINTENANCE'?'UNDER_MAINTENANCE':a.status}});await tx.assetMovement.create({data:{assetId:a.id,type:data.type,fromLocationId:a.currentLocationId,toLocationId:data.toLocationId,movedByUserId:req.user!.id,remarks:data.remarks}});return row});await audit(req,'MOVE','Asset',a.id,a,updated);res.json(updated);});
async function validateRefs(b:any,org:string){const checks=await Promise.all([prisma.product.findFirst({where:{id:b.productId,organizationId:org}}),prisma.assetCategory.findFirst({where:{id:b.categoryId,organizationId:org}}),b.vendorId?prisma.vendor.findFirst({where:{id:b.vendorId,organizationId:org}}):true,b.purchaseOrderId?prisma.purchaseOrder.findFirst({where:{id:b.purchaseOrderId,organizationId:org}}):true,b.departmentId?prisma.department.findFirst({where:{id:b.departmentId,organizationId:org}}):true,b.currentLocationId?prisma.location.findFirst({where:{id:b.currentLocationId,organizationId:org}}):true,b.custodianId?prisma.user.findFirst({where:{id:b.custodianId,organizationId:org}}):true]);if(checks.some(x=>!x))throw new AppError(400,'One or more referenced records do not belong to your organization');if((checks[0] as any).categoryId!==b.categoryId)throw new AppError(400,'Product does not belong to the selected category')}
